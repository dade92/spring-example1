server:
  servlet:
    context-path: /api

spring:
  datasource:
    spring-example:
      host: localhost
      port: 3306
      database: spring-example
      username: ${MYSQL_USERNAME}
      password: ${MYSQL_PASSWORD}
test:
  url: myPersonalUrl

products:
  url: http://localhost:8081
